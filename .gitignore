# runtime competition state — never commit scores
data.json
data.json.tmp

# local backups
*.bak

# large regenerable challenge artifacts — rebuild with: python tools/make_artifacts.py
challenges/17/access.log
challenges/18/mini.dmp
